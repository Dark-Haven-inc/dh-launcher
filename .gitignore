## .NET
bin/
obj/
*.user
artifacts/
[Dd]ebug/
[Rr]elease/
.vs/
*.binlog

## Rider / VS Code
.idea/
.vscode/

## Launcher runtime data / test scratch
/scratch/
*.db
*.db-shm
*.db-wal
!tests/**/fixtures/*.db

## OS
.DS_Store
Thumbs.db

## Bundled engine zips are large — kept as assets on the `engine-bundles` release, not in git
## (see src/DarkHaven.App/bundled-engines/README.md). manifest.json pins each one's SHA-256.
src/DarkHaven.App/bundled-engines/*.zip
src/DarkHaven.App/bundled-engines/*.zip.sig
